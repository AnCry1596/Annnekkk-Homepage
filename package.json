{
  "name": "annnekkk-homepage",
  "version": "2.0.21",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "build": "npm --prefix nuxt install && npm --prefix nuxt run build",
    "seed": "node --env-file=.env seed.js"
  },
  "dependencies": {
    "@fastify/multipart": "^10.1.0",
    "@fastify/static": "^9.3.0",
    "fastify": "^5.10.0",
    "mongodb": "^6.12.0"
  }
}
